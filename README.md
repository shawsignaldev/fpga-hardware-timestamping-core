# FPGA Hardware Timestamping Core

A timestamp normalization and ordering model for FPGA market-data pipelines. It handles clock offsets, monotonic ordering checks, and skew reporting across feed channels.
